name: test-ssla-certs
on:
  push:
    branches:
      - main

jobs:   
  ssla-certs:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Script Execution For Certificate Operations
      run: |
        timestamp=$(date +%Y%m%d%H%M%S)
        Dirname=${timestamp}-"~!@#$%^-_=+,."
        create=$(mkdir -p ./opsjob/${Dirname})
        echo "$create"